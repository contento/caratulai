{
  "name": "caratulai",
  "version": "0.0.0",
  "private": true,
  "description": "Alien image generator — concepts to simple vector images in fundamental palettes",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "samples": "npx tsx scripts/generate-samples.ts",
    "samples:live": "npx tsx scripts/generate-samples.ts --provider=openrouter",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "turbo": "^2.10.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
